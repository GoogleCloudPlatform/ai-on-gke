apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: jetstream-podmonitoring
  namespace: default
spec:
  endpoints:
  - interval: 1s
    path: "/"
    port: ${metrics_port}
  targetLabels:
    metadata:
    - pod
    - container
    - node