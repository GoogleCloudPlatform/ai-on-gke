---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ${pvc_name}
  labels:
  %{~ for key, val in labels ~}
    ${key}: ${val}
  %{~ endfor ~}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: ${pv_name}
  resources:
    requests:
      storage: ${capacity}
