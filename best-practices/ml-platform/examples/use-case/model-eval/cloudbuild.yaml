steps:
- name: 'gcr.io/cloud-builders/docker'
  args:
  - build
  - -t
  - us-docker.pkg.dev/${PROJECT_ID}/llm/validate:${_VERSION}
  - .
  dir: "src"
images:
  - us-docker.pkg.dev/${PROJECT_ID}/llm/validate:${_VERSION}
substitutions:
  _VERSION: "v1.0.0"
