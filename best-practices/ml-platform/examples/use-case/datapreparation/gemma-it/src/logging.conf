[loggers]
keys = root 

[handlers]
keys = json

[formatters]
keys = json

[logger_root]
handlers = json
level = INFO

[handler_json]
class = StreamHandler

formatter = json
args = (sys.stdout,)

[formatter_json]
format = %(levelname)s 
class = pythonjsonlogger.jsonlogger.JsonFormatter
