tracking:
  auth:
    enabled: false
  service:
    type: ClusterIP
minio:
  enabled: false
externalGCS:
  bucket: "<BUCKET_NAME>"
  googleCloudProject: "<PROJECT_ID>"
  useCredentialsInSecret: true
  existingSecret: "gcs-credentials"
  existingSecretKey: "gcloud-application-credentials.json"
  serveArtifacts: true
