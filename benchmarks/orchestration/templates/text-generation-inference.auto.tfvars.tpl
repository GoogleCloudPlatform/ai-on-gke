credentials_config = {
  fleet_host = FLEET_HOST
}

#terraform output -json  | jq '."project_id".value'
project_id = PROJECT_ID

hugging_face_secret = HUGGING_FACE_SECRET

namespace = NAMESPACE_NAME
ksa       = KSA_NAME
