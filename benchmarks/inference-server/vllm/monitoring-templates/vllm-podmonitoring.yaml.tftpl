apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: "vllm-podmonitoring"
  namespace: ${namespace}
spec:
  selector:
    matchLabels:
      app: vllm
  endpoints:
  - path: /metrics
    port: 8000
    interval: 15s
