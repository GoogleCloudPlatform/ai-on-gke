apiVersion: monitoring.googleapis.com/v1
kind: PodMonitoring
metadata:
  name: "vllm-podmonitoring"
  namespace: ${namespace}
spec:
  selector:
    matchLabels:
      app: vllm
  endpoints:
  - port: metrics
    interval: 15s
