apiVersion: v1
kind: Service
metadata:
  labels:
    app: kuberay-tpu-webhook
  name: kuberay-tpu-webhook
  namespace: {{ .Values.kuberay-tpu-webhook.namespace.name }}
spec:
  type: {{ .Values.kuberay-tpu-webhook.webhook-svc.type }}
  selector:
    app: kuberay-tpu-webhook
  ports:
    - port: {{ .Values.kuberay-tpu-webhook.webhook-svc.port }}
      targetPort: https
      protocol: TCP
      name: https